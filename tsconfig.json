{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "strictNullChecks": true,
    "jsx": "react", // Ensure JSX is set to React
    "skipLibCheck": true // Avoid issues with type definitions
  }
}
